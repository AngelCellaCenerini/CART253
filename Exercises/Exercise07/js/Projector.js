class Projector{
  constructor(x, y){
    this.x = x;
    this.y = y;
    this.width = 50;
    this.height = 80;
    this.radius = 5
  }
  display(){
    // Specified in Subclasses
  }
  mousePressed(){
    // Specified in Subclasses
  }
}
